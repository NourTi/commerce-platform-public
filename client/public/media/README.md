# Included visual media

This directory contains the images and videos referenced by the public landing and product-art source. They are included in the public source release so a standard Vite deployment can serve the same visual treatment without relying on platform-specific object-storage paths.

The production-managed project may serve equivalent media from object storage instead; keep source references and deployment storage policy aligned when making changes.
